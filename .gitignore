.DS_Store
node_modules
frontend/dist
backend/build
.gradle
.idea
.vscode
.m2/

# Build artifacts
backend/target/
frontend/dist/

# Runner (code execution service — removed from roadmap)
runner/node_modules/

# Runtime logs and local run metadata
.run/
backend/.run/
*.log
*.log.*
*.err
*.out
*.pid
*.pid.lock
*.seed
*.stackdump
*.trace
hs_err_pid*.log
replay_pid*.log
core
core.*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
frontend/.run*.log
frontend/.run*.err.log
frontend/.run*.pid

# Test and browser tooling output
coverage/
playwright-report/
test-results/

# Load/performance artifacts and ad-hoc load scripts
artifacts/
frontend/artifacts/

# Internal analytics artifacts (Excel, gap-analysis, RICE)
# Не для коммита — личный аналитический контекст основателя
analytics/

# Local debug tooling output
.playwright-cli/
.playwright-mcp/
.ux-audit/

# Local AI tooling configs (personal, not for repo)
.mcp.json
.opencode.json
.cursorrules
.windsurfrules
CLAUDE.md
GEMINI.md

# Cursor personal settings — ignore everything except shared agent rules
.cursor/*.json
.cursor/mcp*.json

# Claude Code personal settings
.claude/

# AGENTS.md is the Codex/OpenAI agent contract — committed so the whole team can use it

# Local vendored toolchain and ad-hoc debug files
.tools/
frontend/tmp-notes-chat-smoke.mjs
frontend/smoke-debug.png

# Debug/AI-session artifacts — screenshots, diffs, patches created during
# browser-testing or code-review sessions. Never belong in the repo.
/*.png
/*.patch
/*full-diff.txt
/*-diff.txt
