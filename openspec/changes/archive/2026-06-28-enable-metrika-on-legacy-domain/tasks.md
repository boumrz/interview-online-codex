## 1. Analytics Host Allowlist

- [x] 1.1 Update the analytics default allowed hosts to include `interview.vtools.tech` and `interview.domiknote.ru`.
- [x] 1.2 Update the Rspack default `VITE_METRIKA_ALLOWED_HOSTS` value to include both production domains.

## 2. Verification

- [x] 2.1 Add focused E2E coverage for analytics initialization on both production domains and blocking on localhost.
- [x] 2.2 Validate OpenSpec and run focused frontend checks.
