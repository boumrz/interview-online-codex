## 1. Refinement and execution gate

- [x] 1.1 Specification Agent: create proposal, delta spec, design context, and test-first task list. AC: required artifacts exist and initial strict validation passes. Output: specification handoff. Documentation-only; automated behavior test not applicable.
- [x] 1.2 Team Lead, using accepted Product Owner scope and final Architect design: record the bounded execution plan, exact file ownership, labels, dependencies, acceptance levels, and risks. AC: no unresolved P0/P1 input and no API/role/schema expansion. Output: execution-plan.md. Depends on 1.1; documentation-only. Final design.md and architecture-review.yaml read and reconciled.
- [x] 1.3 Prompt/Task Auditor: audit every task against execution-plan.md and strict OpenSpec validation. AC: written ready verdict, actionable ownership and dependencies, and test-first gates. Output: task-audit.yaml. Depends on 1.2; no production or test edits before this gate.

## 2. Acceptance before implementation

- [x] 2.1 Root, Developer responsibility: extend only frontend/tests/e2e/hr/e2e-hr-cabinet.mjs with focused tests named `participant HR`. Cover candidate-link admission, manager assignment without UUID input, ordinary/anonymous eligibility, pending/error/retry, assigned state, cabinet uniqueness, non-owner/promoted-guest action, demotion/reconnect/reassignment, and stale completion. AC: focused run fails because the new action is missing, not infrastructure; record RED command/assertion in verification.md. Depends on 1.3. Test level: E2E.
- [x] 2.2 Root, Developer responsibility: create only backend/src/test/kotlin/com/interviewonline/controller/HrParticipantProjectionIntegrationTest.kt. Cover stored HR vs ordinary/anonymous eligibility, spoof resistance, reconnect and aggregation of multiple sessions. Map existing HR integration coverage for unchanged assignment guards, guest/non-owner authority, revocation, idempotency and archive denial; add focused cases only for uncovered required boundaries. AC: new server eligibility assertion fails on current production; record RED plus coverage mapping. Depends on 1.3. Integration exception: these server payload/provenance and direct authorization boundaries cannot be isolated reliably through UI alone.

## 3. Implementation

- [x] 3.1 Root, Backend Developer responsibility: add server-derived `isHr` only in CollaborationService.kt and ws/WsMessages.kt as finalized in design.md; aggregate authenticated sessions with any eligible session. AC: 2.2 becomes green, missing/client-claimed identity grants nothing, existing role enum/API/permission transaction path unchanged. Output: scoped backend diff and focused GREEN. Depends on 2.2 RED.
- [x] 3.2 Frontend Developer: update only TopBar.tsx, useRoomSocket.ts, RoomPage.tsx and RoomPage.module.css to carry server eligibility and invoke existing RTK addHrManager with selected participant userId. AC: `Назначить HR` needs canManageRoom plus eligible authenticated target; assigned HR is clear; owner-only canGrantAccess rules for ordinary interviewer assignment/removal remain unchanged. Root reruns prewritten acceptance flow to GREEN. Depends on 2.1 RED; complete runtime proof also depends on 3.1.
- [x] 3.3 Same Frontend Developer, sequential after 3.2: finish pending/error/retry and request lifecycle in the same four files. AC: duplicate submissions and the target's ordinary role action blocked while pending; success uses server state; errors remain visible/retryable; current-context 410 invokes the existing terminal callback; generation/abort guards invalidate late feedback after account/room/manager-session changes, including loss and regain of authority. Output: scoped frontend diff; root verifies prewritten lifecycle E2E assertions. Depends on 2.1 RED and 3.2.

## 4. Verification and completion

- [x] 4.1 Root, verification responsibility: run focused/full HR E2E, roles and account-switch E2E, full Maven backend suite, frontend typecheck/build; record exact commands, counts, source/runtime identity, RED/GREEN evidence, and relevant baseline coverage in verification.md. AC: all pass on final sources/runtime with no unexplained failures. Depends on 3.1 and 3.3.
- [x] 4.2 Root coordinates Solution Reviewer and Security/Reliability independent reports. AC: scoped diff from output/hr-participant-baseline reviewed; current authority, eligibility provenance, guest inviter, reconnect/revocation, stale completion and archive boundaries have no unresolved blocking findings. Outputs: solution-review.yaml and security-review.yaml. Depends on 4.1; review-only task.
- [x] 4.3 Root coordinates QA evidence verification, then Test Reviewer/UX Critic coverage and interface reviews, then Product Owner final acceptance. AC: all eight product criteria mapped to passing evidence, clear menu states and no unresolved blocking findings. Outputs: QA, test/UX review and product-acceptance artifacts. Depends on 4.2; reviews use existing evidence unless a new concern warrants a targeted check.
- [x] 4.4 Root: reconcile artifacts/tasks/evidence, strict validation and git diff --check; archive with main-spec synchronization. AC: accepted hr-room-tracking includes both new requirements, archived report exists and all gates accurately reflect completion. Depends on 4.3. Documentation-only; no additional behavioral tests required.
