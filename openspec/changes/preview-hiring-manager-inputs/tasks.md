## 1. Specification and architecture gates

- [x] 1.1 **Product Owner scope acceptance** — Confirmed 2026-09-07: preview begins only for a complete UUID after the specified 300 ms debounce; an eligible target is shown only as `Будет добавлен: {displayName}`; unavailable targets use exactly `Нанимающий менеджер не найден или недоступен`; local incomplete tokens show `Введите полный UUID нанимающего менеджера` and do not claim unavailability; preview is advisory and does not gate submit; and public/guest flows, name search, and directory behavior stay out of scope. Product decision is recorded in `proposal.md` and `specs/hiring-manager-input-preview/spec.md`; documentation review only, no automated test applies.
- [x] 1.2 **Architect API/security contract** — Selected read-only `POST /api/me/hiring-manager-preview` with strict `{ invitationId }` request, minimal `{ normalizedId, displayName }` success DTO, opaque `404` unavailable result, defined `400`/`401` behavior, `Cache-Control: no-store`, `Referrer-Policy: no-referrer`, and inherited edge-control `429` behavior in `design.md` decision 1. The contract exposes no target data except eligible `{ normalizedId, displayName }`, has uniform unavailable semantics, and does not change `POST /api/rooms` or public creation; documentation/contract review only, no automated test applies.
- [x] 1.3 **Prompt/Task Auditor gate** — Audited 2026-09-07 after 1.1–1.2. Added explicit RED E2E coverage for stale account/navigation context and `400`/`401`/`429`/transport mapping, made the no-requester-role-gate integration fixture explicit, and added log/telemetry review scope. `npx --yes @fission-ai/openspec@latest validate preview-hiring-manager-inputs --strict` passes. Every executable production task now depends on an explicitly named RED E2E or backend-integration test; process review only.

## 2. Acceptance tests authored and observed RED before production changes

- [x] 2.1 **[RED E2E] Authenticated form preview, correction, and late-result isolation** — **Owner:** QA. **Files owned:** `frontend/tests/e2e/hr/e2e-hr-cabinet.mjs` only. Add a real-browser authenticated-dashboard journey that enters an eligible UUID and observes accessible `Проверяем нанимающего менеджера…` then `Будет добавлен: {displayName}` feedback; enters a complete unknown/ineligible UUID and observes only `Нанимающий менеджер не найден или недоступен`; corrects it without losing title, tasks, or other IDs; and proves a partial UUID shows `Введите полный UUID нанимающего менеджера` without requesting lookup. Include duplicate/case-normalized IDs. Delay a lookup, replace or remove its ID before it completes, submit the corrected valid draft exactly once, and assert exactly one room/assignment is created while the late completion never restores a stale name/result or overwrites confirmed creation state. With a delayed response, also switch the authenticated account and navigate or unmount the dashboard; neither successor context may render the former response. Exercise controlled `400`, `401`, `429`, and transport-failure responses: each must remain a non-positive, non-name state and must not render either `Будет добавлен: {displayName}` or `Нанимающий менеджер не найден или недоступен`; a later current response after a retryable failure may replace that failure state. Assert that preview state itself does not disable an otherwise valid create action. Run `E2E_BASE_URL=http://localhost:5173 E2E_API_URL=http://localhost:8080/api npm --prefix frontend run e2e:hr-cabinet`; record the RED failure caused by absent preview behavior, not fixture/runtime setup. **Test level:** E2E.
- [x] 2.2 **[RED E2E] No guest/public preview surface** — **Owner:** QA. **Files owned:** `frontend/tests/e2e/hr/e2e-hr-cabinet.mjs` only, sequential after 2.1. Extend the browser suite to assert public/guest creation renders no target preview control and makes no lookup call, while authenticated creation still submits only the established `hiringManagerIds` list. Run the 2.1 command and record its RED result. **Test level:** E2E; direct public-route enforcement is covered in 2.3.
- [x] 2.3 **[RED backend integration] Lookup authorization, privacy, fixed method, and no writes** — **Owner:** QA. **Files owned:** new `backend/src/test/kotlin/com/interviewonline/controller/HiringManagerPreviewIntegrationTest.kt` only. Write direct tests for `POST /api/me/hiring-manager-preview`: eligible `200` positive-result minimalism where the requester deliberately does **not** have hiring-manager capability (proving there is no requester-role gate); strict malformed/extra-field and raw duplicate-`invitationId` `400`; unauthenticated/forged `401`; same-body/status/header unavailable `404` for unknown/non-eligible/changed targets; a controlled persistence failure with generic error-only `503` and no-store/referrer privacy headers; the same personal path rejects `GET`, and no public/guest lookup route resolves; `Cache-Control: no-store` and `Referrer-Policy: no-referrer` on every defined response; and proof that lookup writes no room, membership, tracking, profile, or realtime state. Run `mvn -f backend/pom.xml -Dtest=HiringManagerPreviewIntegrationTest test`; record the RED failure caused by the missing contract. **Test level:** backend integration exception because server auth/error/persistence boundaries cannot be isolated by browser E2E.
- [x] 2.4 **[RED backend integration] Preview/create revalidation and atomicity** — **Owner:** QA. **Files owned:** `backend/src/test/kotlin/com/interviewonline/controller/HrRoomTrackingIntegrationTest.kt` only, sequential after 2.3. Add a test that receives a positive preview, changes stored target eligibility before authenticated create, then asserts the existing atomic create failure and no partial room, membership, task, or assignment writes. Run `mvn -f backend/pom.xml -Dtest=HiringManagerPreviewIntegrationTest,HrRoomTrackingIntegrationTest test`; record the RED failure due to missing preview behavior. **Test level:** backend integration exception for deterministic capability-change/create ordering and persistence assertions; late browser response isolation is covered by 2.1.

## 3. Server-authoritative lookup after RED evidence

- [x] 3.1 **[GREEN backend] Implement the Architect-approved preview contract** — **Owner:** Developer. **Files owned:** new `HiringManagerPreviewController`, `HiringManagerPreviewService`, strict preview DTOs, the minimum `UserRepository` read seam, and the endpoint-specific error/header seam needed to place no-store/referrer headers on body-binding `400` and generic persistence `503` responses; `RoomController.kt`, `RoomService.kt`, public DTOs, migrations, and room-role models are out of scope. Implement read-only `POST /api/me/hiring-manager-preview` with current stored account data, strict duplicate-member-rejecting body boundary, positive `{ normalizedId, displayName }` minimalism, uniform unavailable `404`, defined `400`/`401`, generic error-only `503`, no write/SSE effects, and prescribed no-store/referrer headers. Verify `mvn -f backend/pom.xml -Dtest=HiringManagerPreviewIntegrationTest test` passes. **Test level:** backend integration exception. **Dependencies:** 1.2, 2.3.
- [x] 3.2 **[GREEN backend] Preserve server-first creation authority under preview races** — **Owner:** Developer. **Files owned:** only the minimum architect-approved server integration seam if needed; do not duplicate target validation or alter `POST /api/rooms` success semantics. Ensure a lookup has no authority over creation, current capability is rechecked by the established atomic create transaction, and delayed lookup output cannot create/alter room state. Verify `mvn -f backend/pom.xml -Dtest=HiringManagerPreviewIntegrationTest,HrRoomTrackingIntegrationTest test` passes. **Test level:** backend integration exception. **Dependencies:** 2.4, 3.1.
- [x] 3.3 **Server authorization/reliability regression proof** — **Owner:** QA. **Files owned:** no production files. Run `mvn -f backend/pom.xml -Dtest=HiringManagerPreviewIntegrationTest,HrRoomTrackingIntegrationTest,HrAccountProfileIntegrationTest test` and `mvn -f backend/pom.xml test`. Verify the lookup is denied/opaque for direct unauthorized and public calls, post-creation assignment/removal and opt-out remain intact, and no database migration/realtime regressions occur. **Test level:** integration regression. **Dependencies:** 3.1, 3.2.

## 4. Dashboard preview after RED evidence

- [x] 4.1 **[GREEN frontend] Add a non-persistent typed lookup client seam** — **Owner:** Developer. **Files owned:** `frontend/src/services/api.ts`, `frontend/src/types/index.ts`, and any narrowly scoped frontend API type helper. Add only the abortable, non-fixed-key authenticated `POST /api/me/hiring-manager-preview` request and its `{ invitationId }` / `{ normalizedId, displayName }` types; it must not add a public-client request type, local-storage entry, persistent shared result cache, analytics payload containing UUID/name, or create-request display-name field. Treat only opaque `404` as unavailable and `400`/`401`/`429`/transport failures as non-positive non-name states. Verify `npm --prefix frontend run typecheck` passes. **Test level:** TypeScript contract support for E2E/integration-covered behavior. **Dependencies:** 1.2, 2.1–2.3, 3.1.
- [x] 4.2 **[GREEN frontend] Render accessible current preview states in room creation** — **Owner:** Developer. **Files owned:** `frontend/src/pages/DashboardPage.tsx`, `frontend/src/pages/dashboard/CreateRoomSection.tsx`, and their existing CSS module only if layout needs it. Keep the raw optional IDs and current create semantics, then render per-unique-ID `Проверяем нанимающего менеджера…`, `Будет добавлен: {displayName}`, `Нанимающий менеджер не найден или недоступен`, and `Введите полный UUID нанимающего менеджера` states with accessible status/error semantics. Correction/deletion/deduplication must preserve unrelated draft fields and remove obsolete results. Preview feedback itself must not disable an otherwise valid create action. Verify the 2.1 E2E command and `npm --prefix frontend run typecheck` pass. **Test level:** E2E. **Dependencies:** 2.1, 3.1, 4.1.
- [x] 4.3 **[GREEN frontend] Guard debounce, cancellation, and stale context** — **Owner:** Developer. **Files owned:** `frontend/src/pages/DashboardPage.tsx` and any dedicated local preview-state helper created for this feature; do not change guest/public form components. Implement the 300 ms debounce, per-normalized-ID de-duplication, abort/generation guard, and invalidation on edit, logout/account switch, navigation, and unmount. A pending/late lookup must not block or overwrite server-confirmed creation state. Verify the delayed-response, duplicate, account-switch, navigation/unmount, and failure-mapping assertions in the 2.1 E2E command pass. **Test level:** E2E. **Dependencies:** 2.1, 4.1, 4.2.
- [x] 4.4 **[GREEN frontend] Preserve no-public exposure and create compatibility** — **Owner:** Developer. **Files owned:** only authenticated-dashboard/API seams necessary to correct the 2.2 journey; public/guest create components, request types, and routes must not gain the feature. Verify the 2.2 E2E command passes and the existing authenticated create journey still sends only normalized IDs to `POST /api/rooms`. **Test level:** E2E. **Dependencies:** 2.2, 3.2, 4.2–4.3.

## 5. Integrated verification and release gates

- [x] 5.1 **Acceptance GREEN and frontend regressions** — **Owner:** QA. Run the complete `e2e:hr-cabinet` suite from 2.1, `npm --prefix frontend run e2e:roles`, `npm --prefix frontend run e2e:account-binding`, `npm --prefix frontend run e2e:account-switch`, `npm --prefix frontend run typecheck`, and `npm --prefix frontend run build`. Verify eligible/unavailable preview, correction, guest absence, delayed responses, stale account/navigation contexts, failure mapping, profile capability changes, and ordinary room flows pass on one runtime. **Test level:** E2E plus build/type regression. **Dependencies:** 3.3, 4.2–4.4.
- [x] 5.2 **Solution and Security/Reliability review** — **Owner:** Team Lead. Have `solution-reviewer-agent` review the selected API/client request-generation design and `security-reliability-agent` review authentication, UUID-only discovery resistance, uniform unavailable data, duplicate-field and generic-503 privacy boundaries, no-cache/no-write guarantees, absence of raw invitation UUID/display-name application logs or telemetry, stale response handling, and existing rate-limit/error-policy fit. Verify no change-local blocking finding remains. Record that session TTL/server-side revoke is a separate platform-security backlog and that an operator must confirm production authenticated ingress limits before deployment; neither must be presented as implemented by this change. Review only, no automated test applies. **Dependencies:** 3.3, 4.4.
- [x] 5.3 **Test, UX, and Product Owner acceptance** — **Owner:** Team Lead. Have `test-reviewer-agent` validate the E2E/integration race and privacy coverage, `ux-critic-agent` validate Russian accessible pending/success/error states and correction flow, and `product-owner-agent` accept the in-scope behavior. Verify all P0 findings are resolved; review only, no automated test applies. **Dependencies:** 5.1, 5.2.
- [x] 5.4 **Reconcile OpenSpec and final validation** — **Owner:** Specification Agent. Update only completed task checkboxes and command evidence, run `npx --yes @fission-ai/openspec@latest validate preview-hiring-manager-inputs --strict` and `git diff --check`, and confirm no unrelated worktree changes were reverted. Sync or archive the completed predecessor `rename-hr-to-hiring-manager` before archiving this dependent change; archive remains a separate explicit release decision. **Dependencies:** 5.3.

## Verification evidence

- **RED before implementation:** the missing endpoint produced `404` instead of the required preview responses, and the dashboard E2E failed waiting for `Проверяем нанимающего менеджера…`. The retained-pending-ID race and the retryable-copy mutation were each also reproduced RED before their focused fixes.
- **Backend GREEN:** `env JAVA_HOME=/Library/Java/JavaVirtualMachines/temurin-17.jdk/Contents/Home PATH=/Library/Java/JavaVirtualMachines/temurin-17.jdk/Contents/Home/bin:$PATH mvn -f backend/pom.xml -Dtest=HiringManagerPreviewIntegrationTest,HrRoomTrackingIntegrationTest,HrAccountProfileIntegrationTest test` passed 31 tests; `mvn -f backend/pom.xml test` passed 110 tests. The pre-existing workspace-snapshot test cleanup is now ordered from task templates through categories before users.
- **Frontend GREEN:** `E2E_BASE_URL=http://localhost:5173 E2E_API_URL=http://localhost:8080/api npm --prefix frontend run e2e:hr-cabinet` passed 33 tests against the local app. `npm --prefix frontend run e2e:roles`, `e2e:account-binding`, `e2e:account-switch`, `typecheck`, and `build` passed; build has only pre-existing asset-size warnings.
- **Focused mutation check:** changing the exact retryable Russian copy to the predecessor wording caused the focused retryable-preview E2E to fail; restoring the accepted copy made it pass.
- **Review gates:** solution and security/reliability re-reviews approved; test coverage was sufficient; Product Owner accepted the feature. UX recorded a non-blocking P1 future enhancement: an explicit retry button beside retryable feedback.
