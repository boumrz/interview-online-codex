## 1. Scope, architecture, and task-quality gates

- [x] 1.1 **Product Owner acceptance** — Confirm the bounded product decisions in `proposal.md` and delta specs: all user-facing terminology becomes “нанимающий менеджер”; a self-disable removes capability-only access but does not globally demote existing room memberships; authenticated dashboard creation accepts optional multiple copied invitation UUIDs and is all-or-nothing. Verify no P0 product question remains and record scope/value acceptance. Accepted 2026-09-07: multiple targets retain the established multi-manager workflow at creation and avoid a forced post-creation assignment; all other directory/role/public-flow expansion remains excluded. Documentation review; automated behavior test not applicable.
- [x] 1.2 **Architect contract** — Finalized in `design.md` on 2026-09-07: authenticated `POST /api/rooms` adds only optional `hiringManagerIds: List<String>` and retains the `200 RoomResponse`; public creation explicitly rejects that field. The contract specifies canonical UUID normalization/deduplication, deterministic target/profile row locking, one all-or-nothing room/task/membership/tracking transaction, uniform privacy-safe target failures, explicit self-only profile `false`, confirmed-state/cache behavior, SSE/reconnect continuity, and server-first rollout. It adds no role, schema migration, directory, public-flow assignment, or client-authoritative authorization. Documentation review; automated behavior test not applicable.
- [x] 1.3 **Team Lead decomposition** — Completed 2026-09-07 after 1.1–1.2. The executable plan below assigns a single owner, owned files, inputs/outputs, acceptance level, command, and explicit RED → GREEN dependency to every implementation slice. **Milestones:** M1 gate + RED evidence (1.4, 2.1–2.5); M2 server-authoritative behavior (3.1–3.3); M3 dashboard and product-copy behavior (4.1–4.5); M4 integrated verification/release gates (5.1–5.4). **Ownership rule:** `DashboardPage.tsx` has one sequential frontend owner: 4.2 completes before 4.3; 4.4 must not edit it. `e2e-hr-cabinet.mjs` likewise has one sequential QA owner: 2.1 completes before 2.2. **Review assignment:** `test-reviewer-agent` reviews RED/acceptance-test coverage (2.1–2.6, 5.1); `solution-reviewer-agent` reviews production slices (3.1–3.2, 4.1–4.4); `security-reliability-agent` reviews authorization, privacy, atomicity, and reconnect boundaries (3.1–3.3, 5.2); `ux-critic-agent` reviews rendered terminology and interaction states (4.2–4.4, 5.3); `product-owner-agent` accepts product scope (5.3). **Critical path:** `1.4 → {2.1,2.3,2.4,2.5}; 2.1 → 2.2; {2.1,2.3,2.4} → 3.1 → 4.2; {2.2,2.5} → 3.2 → 4.3; {2.1,2.2} → 4.1; {2.3,4.2,4.3} → 4.4; {3.3,4.4} → 4.5 → 5.1`. Documentation/process review; no automated behavior test applies.
- [x] 1.4 **Prompt/Task Auditor gate** — Audit this task list, final architecture contract, and `npx --yes @fission-ai/openspec@latest validate rename-hr-to-hiring-manager --strict`. Verify a ready verdict before any production or test edit; record blocking gaps, if any. Documentation review; automated behavior test not applicable.

## 2. Acceptance tests authored and observed RED before production changes

- [x] 2.1 **[Stream G] RED E2E: profile toggle and immediate loss of cabinet access** — **Owner:** `qa-agent`. **Files owned:** `frontend/tests/e2e/hr/e2e-hr-cabinet.mjs` (do not edit application files). **Inputs:** accepted profile scenarios in `hr-account-profile/spec.md` and design decision 2. **Output/AC:** a real-browser scenario asserts the exact accessible product term for registration and profile, enable → confirmed profile update → disable → reload/login, cabinet navigation disappearing, direct protected cabinet/list/detail/export denial, unchanged pre-existing room role/history, and a retryable failed save that retains the confirmed account state. **RED command:** `E2E_BASE_URL=http://localhost:5173 E2E_API_URL=http://localhost:18080/api npm --prefix frontend run e2e:hr-cabinet`; record the assertion failure caused by the missing behavior/copy, not fixture/runtime setup. **Test level:** E2E; the direct protected-call assertion supplements browser visibility. **Dependencies:** 1.4. **GREEN consumers:** 3.1, 4.1, 4.2.
- [x] 2.2 **[Stream G] RED E2E: dashboard creation-time assignment** — **Owner:** `qa-agent` (same sequential file owner as 2.1). **Files owned:** `frontend/tests/e2e/hr/e2e-hr-cabinet.mjs` only. **Inputs:** `hr-room-tracking/spec.md`, design decisions 3–5, 2.1 completed. **Output/AC:** browser scenarios for two valid pasted invitation UUIDs, omitted-target compatibility, invalid/ineligible privacy-safe feedback with title/tasks/raw IDs retained, client duplicate normalization, disabled duplicate submit, confirmed-only navigation, and absence of a target field in the public/guest create flow. **RED command:** `E2E_BASE_URL=http://localhost:5173 E2E_API_URL=http://localhost:18080/api npm --prefix frontend run e2e:hr-cabinet`; record an assertion failure due to absent creation-time behavior. **Test level:** E2E. **Dependencies:** 1.4, 2.1. **GREEN consumers:** 3.2, 4.1, 4.3.
- [x] 2.3 **[Stream G] RED backend integration: explicit self-disable and server reauthorization** — **Owner:** `qa-agent`. **Files owned:** `backend/src/test/kotlin/com/interviewonline/controller/HrAccountProfileIntegrationTest.kt` only. **Inputs:** profile delta spec and design decision 2. **Output/AC:** focused direct-request coverage proves `isHr:false` survives an auth refresh, remains self-only despite forged identity/role data, denies cabinet list/detail/export after opt-out, leaves room role/history untouched, and permits re-enable. **RED command:** `mvn -f backend/pom.xml -Dtest=HrAccountProfileIntegrationTest test`; record failure because false is rejected/current access remains available. **Test level:** backend integration exception—persistence, authorization, and forged requests are not independently provable from E2E. **Dependencies:** 1.4. **GREEN consumer:** 3.1.
- [x] 2.4 **[Stream G] RED backend integration: successful creation assignment semantics** — **Owner:** `qa-agent`. **Files owned:** `backend/src/test/kotlin/com/interviewonline/controller/HrRoomTrackingIntegrationTest.kt` only. **Inputs:** room-tracking delta spec and design decisions 3–4. **Output/AC:** direct authenticated `POST /api/rooms` tests prove omitted-list compatibility; two eligible UUIDs (including canonicalized duplicates) create exactly one interviewer membership and one tracked assignment per effective target; owner-as-target follows established tracking semantics; and a target reconnect resolves durable interviewer authority. **RED command:** `mvn -f backend/pom.xml -Dtest=HrRoomTrackingIntegrationTest test`; record missing additive-create failure. **Test level:** backend integration exception—transactional records/reconnect are not isolated by browser flow. **Dependencies:** 1.4. **GREEN consumer:** 3.2.
- [x] 2.5 **[Stream G] RED backend integration: creation rejection, privacy, and atomicity** — **Owner:** `qa-agent`. **Files owned:** `backend/src/test/kotlin/com/interviewonline/controller/HrRoomTrackingIntegrationTest.kt` only (sequential after 2.4). **Inputs:** design decision 3 and 2.4. **Output/AC:** direct tests prove malformed/unknown/ineligible targets share the specified generic 404, malformed structure is 400, public `hiringManagerIds` is rejected rather than ignored, unauthenticated authenticated-create stays 401, and every rejected target list leaves no room/tasks/participants/assignments. Include the eligibility-change race boundary required by the design. **RED command:** `mvn -f backend/pom.xml -Dtest=HrRoomTrackingIntegrationTest test`; record an assertion failure caused by absent contract/atomicity. **Test level:** backend integration exception. **Dependencies:** 1.4, 2.4. **GREEN consumer:** 3.2.
- [x] 2.6 **[Stream G] RED static product-copy audit** — **Owner:** `qa-agent`. **Files owned:** new `frontend/tests/unit/hiringManagerProductCopy.test.ts` only. **Inputs:** proposal terminology decision and the enumerated rendered-copy surfaces: `LoginPage.tsx`, `DashboardPage.tsx`, `dashboard/HrProfileSection.tsx`, `dashboard/dashboardConstants.ts`, `features/hr/CopyHrId.tsx`, `features/room/TopBar.tsx`, `features/room/RoomInterviewPanel.tsx`, and `RoomPage.tsx`. **Output/AC:** a source audit fails on legacy rendered Russian “HR” variants and permits a narrowly documented technical/protocol allowlist (identifiers, routes, CSS tags, fixtures); it must not scan tests. **RED command:** `node --experimental-strip-types --test frontend/tests/unit/hiringManagerProductCopy.test.ts`; record current product-copy failures. **Test level:** frontend unit/source-audit exception—unvisited static error/empty states are disproportionate for E2E; 2.1–2.2 still cover journeys. **Dependencies:** 1.4. **GREEN consumer:** 4.4.

## 3. Backend implementation after RED evidence

- [x] 3.1 **[Stream A] GREEN backend: self-service capability toggle** — **Owner:** `developer-agent`. **Files owned:** `backend/src/main/kotlin/com/interviewonline/service/AuthService.kt`, `backend/src/main/kotlin/com/interviewonline/repository/UserRepository.kt`, `backend/src/main/kotlin/com/interviewonline/service/HrInterviewService.kt`, and `backend/src/main/kotlin/com/interviewonline/service/RoomHrTrackingService.kt`; do not alter migrations, endpoint paths, or room-role models. **Inputs:** 2.3 RED evidence and design decision 2. **Output/AC:** an explicit authenticated false locks/reloads the session account, persists/returns false; omitted `isHr` retains existing state; protected cabinet/list/detail/export/tracking recheck stored capability; server errors use product vocabulary; and no room membership/history write occurs. **GREEN command:** `mvn -f backend/pom.xml -Dtest=HrAccountProfileIntegrationTest test`. **Test level:** backend integration exception. **Dependencies:** 2.3. **Downstream:** 3.3, 4.2.
- [x] 3.2 **[Stream B] GREEN backend: atomic authenticated room creation with hiring-manager targets** — **Owner:** `developer-agent`. **Files owned:** `backend/src/main/kotlin/com/interviewonline/dto/RoomDto.kt`, `backend/src/main/kotlin/com/interviewonline/controller/RoomController.kt`, `backend/src/main/kotlin/com/interviewonline/service/RoomService.kt`, `backend/src/main/kotlin/com/interviewonline/service/RoomHrTrackingService.kt`, and `backend/src/main/kotlin/com/interviewonline/repository/UserRepository.kt`; no schema migration/new role/public target field. **Inputs:** 2.4–2.5 RED evidence and design decisions 3–4. **Output/AC:** only authenticated create accepts optional list; server canonicalizes/locks target rows in UUID order, revalidates stored eligibility, reuses one membership/tracking primitive, and commits room/tasks/effective assignments together. Public create rejects the field, malformed and unavailable target envelopes meet the specified privacy contract, and response remains `RoomResponse`. **GREEN command:** `mvn -f backend/pom.xml -Dtest=HrRoomTrackingIntegrationTest test`. **Test level:** backend integration exception. **Dependencies:** 2.4, 2.5. **Downstream:** 3.3, 4.3.
- [x] 3.3 **[Stream G] Backend regression/security proof** — **Owner:** `qa-agent`. **Files owned:** no production files; append exact command results only to this task's completion evidence. **Inputs:** 3.1–3.2 green commits. **Output/AC:** documented passing proof for candidate/private-note isolation, established post-creation assignment/removal, archive denial, reconnect authority, omitted old authenticated payloads, and unchanged public flow. **Commands:** `mvn -f backend/pom.xml -Dtest=HrAccountProfileIntegrationTest,HrRoomTrackingIntegrationTest test` then `mvn -f backend/pom.xml test`. **Test level:** integration regression. **Dependencies:** 3.1, 3.2. **Downstream:** 5.1.

## 4. Frontend implementation after RED evidence

- [x] 4.1 **[Stream A/B] Frontend contract and cache seam** — **Owner:** `developer-agent`. **Files owned:** `frontend/src/services/api.ts`, `frontend/src/types/index.ts`, `frontend/src/features/auth/authSlice.ts`; do not edit `DashboardPage.tsx`. **Inputs:** 2.1–2.2 RED evidence and design decisions 2–3. **Output/AC:** typed authenticated create accepts an optional `hiringManagerIds` list while guest create has no such property; profile mutation accepts/sends explicit false; confirmed profile handling can invalidate `HrInterviews` and does not optimistically alter account state. **GREEN command:** `npm --prefix frontend run typecheck`. **Test level:** TypeScript contract support for the E2E-covered behavior. **Dependencies:** 2.1, 2.2. **Downstream:** 4.2, 4.3.
- [x] 4.2 **[Stream A/F] GREEN frontend: controlled profile capability UI** — **Owner:** `developer-agent`, the sole owner of `DashboardPage.tsx` until 4.3 completes. **Files owned:** `frontend/src/pages/DashboardPage.tsx` and `frontend/src/pages/dashboard/HrProfileSection.tsx`. **Inputs:** 2.1 RED, 3.1 green, 4.1. **Output/AC:** one controlled checkbox exactly labeled “Я нанимающий менеджер” supports checked/unchecked saves, accessible pending/success/retryable errors, dispatches only confirmed `UserDto`, invalidates capability-only cabinet data/aborts export on disable, redirects from the cabinet, and leaves ordinary room access untouched. This task also replaces every rendered product string in its two files. **GREEN commands:** the 2.1 E2E command, `npm --prefix frontend run typecheck`, `npm --prefix frontend run build`. **Test level:** E2E. **Dependencies:** 2.1, 3.1, 4.1. **Downstream:** 4.3, 4.4.
- [x] 4.3 **[Stream B/F] GREEN frontend: dashboard creation-time assignment** — **Owner:** `developer-agent`, continuing the same sequential `DashboardPage.tsx` ownership after 4.2. **Files owned:** `frontend/src/pages/DashboardPage.tsx` and `frontend/src/pages/dashboard/CreateRoomSection.tsx`. **Inputs:** 2.2 RED, 3.2 green, 4.1, 4.2 complete. **Output/AC:** authenticated dashboard form offers an accessible optional pasted-UUID list named “Нанимающие менеджеры”, normalizes draft duplicates, disables overlap, preserves title/tasks/raw IDs after non-success, displays privacy-safe retryable error, and navigates only after confirmed create. No guest/public surface or request type gains the field. This task also replaces rendered product strings in `DashboardPage.tsx`. **GREEN commands:** the 2.2 E2E command, `npm --prefix frontend run typecheck`, `npm --prefix frontend run build`. **Test level:** E2E. **Dependencies:** 2.2, 3.2, 4.1, 4.2. **Downstream:** 4.4.
- [x] 4.4 **[Stream C/D/F] GREEN frontend: remaining product terminology** — **Owner:** `developer-agent`; may run after 4.2 but must not edit `DashboardPage.tsx` or `HrProfileSection.tsx`. **Files owned:** `frontend/src/pages/LoginPage.tsx`, `frontend/src/pages/dashboard/dashboardConstants.ts`, `frontend/src/features/hr/CopyHrId.tsx`, `frontend/src/features/room/TopBar.tsx`, `frontend/src/features/room/RoomInterviewPanel.tsx`, and `frontend/src/pages/RoomPage.tsx`. **Inputs:** 2.6 RED, finished 4.2–4.3 ownership handoff, terminology specs. **Output/AC:** all rendered labels, titles, help text, notifications, confirmation/error messages, and accessible names in the owned files use “нанимающий менеджер”; endpoints, TypeScript/Kotlin symbols, route segments, non-product protocol text, CSS `hr` selectors, and test fixtures remain compatible. **GREEN commands:** `node --experimental-strip-types --test frontend/tests/unit/hiringManagerProductCopy.test.ts`, the 2.1/2.2 E2E command, `npm --prefix frontend run typecheck`, `npm --prefix frontend run build`. **Test level:** source audit plus E2E. **Dependencies:** 2.6, 4.2, 4.3. **Downstream:** 4.5.
- [x] 4.5 **[Stream G] Frontend regression proof** — **Owner:** `qa-agent`. **Files owned:** no application files; record exact results in completion evidence. **Inputs:** 4.2–4.4. **Output/AC:** all new acceptance journeys and existing owner/interviewer/account switching flows pass on the same runtime, with no stale cabinet restoration after reload/login or public-target control exposure. **Commands:** `E2E_BASE_URL=http://localhost:5173 E2E_API_URL=http://localhost:18080/api npm --prefix frontend run e2e:hr-cabinet`; `npm --prefix frontend run e2e:roles`; `npm --prefix frontend run e2e:account-binding`; `npm --prefix frontend run e2e:account-switch`; `npm --prefix frontend run typecheck`; `npm --prefix frontend run build`. **Test level:** E2E plus build/type regression. **Dependencies:** 4.2, 4.3, 4.4. **Downstream:** 5.1.

## 5. Integrated verification, review, and handoff

- [x] 5.1 **Acceptance GREEN and regressions** — **Owner:** `qa-agent`. Run the full 2.1 command, `npm --prefix frontend run e2e:roles`, `npm --prefix frontend run e2e:account-binding`, `npm --prefix frontend run e2e:account-switch`, `npm --prefix frontend run typecheck`, `npm --prefix frontend run build`, focused Maven commands from 2.3–2.4, and `mvn -f backend/pom.xml test`. Verify all pass on the same backend/frontend runtime and record RED/GREEN evidence. Test level: **E2E plus integration regression**.
- [x] 5.2 **Solution and security/reliability review** — **Owner:** `team-lead-agent`. Have `solution-reviewer-agent` review the cross-module profile/create contract and `security-reliability-agent` verify server-only authorization, privacy-safe target errors, atomicity, stale capability state, cache/reconnect behavior, and absence of public-flow elevation. Verify no blocking finding remains before release. Review-only; automated behavior test not applicable.
- [x] 5.3 **QA, UX, and Product Owner acceptance** — **Owner:** `team-lead-agent`. Have `qa-agent` map every spec scenario to evidence, `test-reviewer-agent` assess E2E/integration/source-audit coverage, `ux-critic-agent` review Russian terms and disabled/error states, and `product-owner-agent` accept delivered scope. Verify all P0 findings are resolved. Review-only; automated behavior test not applicable.
- [x] 5.4 **Reconcile and validate OpenSpec** — **Owner:** `specification-agent`. Update completed checkboxes and verification evidence, run `npx --yes @fission-ai/openspec@latest validate rename-hr-to-hiring-manager --strict` and `git diff --check`, then archive only after all acceptance gates agree. Verify no unrelated working-tree change is reverted. Documentation/process verification; no additional automated behavior test required.

## Verification evidence (2026-09-07)

### RED evidence before production implementation

- Profile E2E failed because the exact hiring-manager capability label was absent (`0 !== 1`); room-creation E2E failed because the authenticated manager-ID control was absent (`0 !== 1`).
- `HrAccountProfileIntegrationTest` failed with `expected 200 but was 400` because an explicit `isHr:false` was rejected.
- `HrRoomTrackingIntegrationTest` failed because supplied target IDs produced no interviewer membership. The rendered-product-copy audit also failed on legacy Russian HR labels.

### Final GREEN evidence on local runtime

- Full HR cabinet E2E against `http://localhost:5173` and `http://localhost:8080/api`: `28 passed, 0 failed`.
- Existing frontend regressions: `e2e:roles`, `e2e:account-binding`, and `e2e:account-switch` passed; `npm --prefix frontend run typecheck` passed; `npm --prefix frontend run build` passed (only existing asset-size warnings).
- JDK 17 focused profile/room integration suite passed `23, 0`; full `mvn -f backend/pom.xml test` passed `102, 0`.
- Added coverage verifies public-form omission and successful room entry, empty target compatibility, canonical multi-target assignment, pending/no-retry and complete draft retention, task rollback, target browser authority, forged creation fields, capability-change/create serialization, profile stale-response resistance, and invitation UUID stability.
- Solution, security/reliability, and UX reviews found no remaining blocking finding. The task remains unarchived intentionally; archival is a separate explicit release action.
