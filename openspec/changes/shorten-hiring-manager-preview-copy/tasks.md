## 1. Scope and task-quality gates

**Single implementation owner:** `developer-agent (root executor)`. Its complete write scope for tasks 2.1–4.1 is `frontend/src/pages/DashboardPage.tsx`, `frontend/src/pages/dashboard/CreateRoomSection.tsx`, `frontend/tests/e2e/hr/e2e-hr-cabinet.mjs`, and `frontend/tests/unit/hiringManagerPreviewCopy.test.ts`. No other production, API/type, role/cabinet, or public/guest files are owned by this change.

- [x] 1.1 **Product Owner copy acceptance** — Confirm the seven exact strings in `specs/hiring-manager-preview-copy/spec.md`, including the Unicode ellipsis and final period, and confirm that the compact terms are confined to the room-creation preview and post-submit invalid-identifier error. Verify no terminology, privacy, role, cabinet, endpoint, or public/guest-flow expansion is introduced. Documentation review; automated behavior test not applicable.
- [x] 1.2 **Team Lead and Prompt/Task Auditor gate** — Confirm `developer-agent (root executor)` is the sole owner for the four explicitly listed files, that the implementation has no backend/API/type ownership, and that it does not overlap unrelated dashboard work. Audit the explicit test-first dependencies below and run `npx --yes @fission-ai/openspec@latest validate shorten-hiring-manager-preview-copy --strict`; record a ready verdict before production edits. Documentation review; automated behavior test not applicable.

## 2. Acceptance tests authored and observed RED before copy edits

- [x] 2.1 **Browser RED: exact compact preview and submission copy** — Extend only `frontend/tests/e2e/hr/e2e-hr-cabinet.mjs` with real-browser assertions for the exact label, placeholder, pending message, incomplete-ID feedback, unavailable result, retryable preview failure, and post-submit invalid-identifier error. Include one successful prospective-assignment path to prove it remains advisory and one public/guest path to prove the control is absent. Verify `E2E_BASE_URL=http://localhost:5173 E2E_API_URL=http://localhost:8080/api npm --prefix frontend run e2e:hr-cabinet` fails because the expected compact copy is not rendered, not due to fixture/runtime failure. **Depends on 1.1 and 1.2.** Test level: **E2E**.
- [x] 2.2 **Static-copy RED: exact literal audit** — Add `frontend/tests/unit/hiringManagerPreviewCopy.test.ts`; it SHALL examine only `frontend/src/pages/DashboardPage.tsx` and `frontend/src/pages/dashboard/CreateRoomSection.tsx`, assert the exact seven strings, and reject longer preview/error literals in those files while allowing technical identifiers. Do not extend the existing cross-surface product-copy audit. From `frontend/`, verify `node --experimental-strip-types --test tests/unit/hiringManagerPreviewCopy.test.ts` fails on the current longer copy. **Depends on 1.1 and 1.2.** Test level: **frontend unit/source-audit exception** because exact punctuation and every fallback literal are disproportionate to exercise in a single browser flow; task 2.1 remains user-observable E2E acceptance.

## 3. Copy-only implementation after RED evidence

- [x] 3.1 **Render the compact copy in existing room-creation preview paths** — Update only `frontend/src/pages/DashboardPage.tsx` and `frontend/src/pages/dashboard/CreateRoomSection.tsx`. Use each exact string from the delta spec for its established state, preserve accessibility roles, raw drafts, correction/retry, preview advisory behavior, submission availability, and post-submit draft retention. Do not add helpers or edit backend, endpoint/type definitions, role/cabinet controls, or public/guest components. Verify task 2.1 becomes green. **Depends on observed RED evidence from both 2.1 and 2.2.** Test level: **E2E**.
- [x] 3.2 **Prove literal precision and type/build compatibility** — From `frontend/`, run `node --experimental-strip-types --test tests/unit/hiringManagerPreviewCopy.test.ts`, then `npm run typecheck` and `npm run build`. Verify the exact ellipsis/punctuation and all seven strings are present only in scope, and no technical contract is changed. **Depends on 3.1.** Test level: **frontend unit/source-audit exception plus build/type regression**.

## 4. Integrated verification and handoff

- [x] 4.1 **Regression GREEN** — Run the task 2.1 E2E command, `npm --prefix frontend run e2e:roles`, `npm --prefix frontend run e2e:account-binding`, `npm --prefix frontend run typecheck`, and `npm --prefix frontend run build`. Verify preview feedback has only the requested copy change, public/guest creation remains free of preview controls, and ordinary role/cabinet flows remain unchanged; record RED/GREEN evidence. **Depends on 3.2.** Test level: **E2E plus frontend regression**.
- [x] 4.2 **Post-GREEN review gates and task completion** — After 4.1, run the review sequence exactly: `solution-reviewer-agent` → `qa-agent` → `test-reviewer-agent` → `ux-critic-agent` → `product-owner-agent` → final `prompt-task-auditor-agent` close. The solution reviewer verifies scope/regression risk; QA verifies executed behavior; the test reviewer verifies E2E/source-audit coverage; the UX critic verifies compact Russian copy and accessible pending/error states; product owner confirms exact wording and scope; the final auditor confirms all evidence is complete. No blocking finding may remain. **Depends on 4.1.** Review-only; automated behavior test not applicable.
- [x] 4.3 **Reconcile and validate OpenSpec** — Update task status/evidence only after all checks pass; run `npx --yes @fission-ai/openspec@latest validate shorten-hiring-manager-preview-copy --strict` and `git diff --check`. Verify no files outside this change were altered or reverted; archive only by explicit release decision. Documentation/process verification; no additional automated behavior test required.
